
"""2- confeccionaruna funcionque le enviamos
el lado de un cuadrado y nos muestresu superficie"""

""" 3-confeccionar una funcion que muestre todos los valores enteros
comprendidos entre los dos parametros.
"""
"""4- confeccionar una funcion que reciba una palabra en minuscula y muestre
cuantas letras "a" tiene.
""
""
5- confecciomnar una funcion que le enviamos mun
entero e imprima si es elvalor es par o no.
"""

def sumar(v1, v2):
    
    suma= v1 + v2
    print("la suma de  los valores es",suma)
    
def calcular_sup_cuadrado(lado):
    
    superficie= lado * lado
    print("la superficie del cuadradoes", superficie)
    
def mostrar_numeros(desde,hasta):
    
     #for x in range(desde,hasta+1):
     #     print(x)
    while desde=hasta:
          print(desde)
          desde= desde + 1
          
def cantidad_da(palabras):
    
    cant = 0
    for i in range(len(palabra)):
        if palabra[x]
        cant= cant + 1
        print("cantidad de vocales ´a´ de las palabras", palabra,"es", cant)

def es_par(numero):
    
    if numero % 2 ==0:
        print(numero,"es par")
    else:
        print(numero,"no es par")
    
    

# []bloque pricipal
sumar(10, 40)
calcular_sup_cuadrado(20)
x=12
calcular_sup_cuadrado(x)
mostrar_numeros(1,5)
cantidad_a("casa")
es_par(10)
es_par(3)
"""
sumar(3,4)
for x in range(19):
sumar(x, x)
valor1=int(input("ingrese primer valor:"))
valor2=int(input("ingrese segundo valor:"))
sumar(valor1, valor2)
"""

   


